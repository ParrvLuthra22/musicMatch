export default function HomePage() {
  return (
    <div>
      <h1>TuneMate Auth Demo</h1>
      <p>Use Signup and Login pages to create an account and obtain a JWT.</p>
      <p>After deployment, this frontend will call the backend at NEXT_PUBLIC_API_URL.</p>
    </div>
  )
}
